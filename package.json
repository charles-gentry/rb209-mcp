{
  "name": "rb209-mcp",
  "version": "0.3.0",
  "license": "MIT",
  "type": "module",
  "bin": {
    "rb209-mcp": "dist/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run sync-version && tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "bundle": "tsx scripts/build-bundle.ts",
    "sync-version": "tsx scripts/sync-version.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@anthropic-ai/mcpb": "^2.1.2",
    "@types/node": "^20.0.0",
    "esbuild": "^0.23.0",
    "msw": "^2.4.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
